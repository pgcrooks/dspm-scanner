datastore:
  local_db:
    enabled: false
    path: "dspm.db"
  memory:
    enabled: true
finder:
  aws:
    enabled: false
    bucket_name: "pgcrooks-dspm"
  local:
    enabled: true
    path: "test/data/"
